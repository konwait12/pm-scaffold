# 产品 UX Skill

负责定义页面骨架（§页面设计）和页面层交互规则（§交互规则）。页面与 IX 规则是必出内容；原型按沟通需要生成。功能清单与功能流程不在此产出，归 `function-description`。

本 Skill 通过内部 `page-design` 子 skill 定义页面/步骤骨架（入口、前置条件、主要内容、操作、下一状态），通过 `interaction-rules` 子 skill 定义页面层交互规则 `IX-XXX`（操作反馈、跳转、弹窗和表单交互）。领域业务规则、权限、校验、异常恢复和验收依据进入 `function-description`。

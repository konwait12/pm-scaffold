# 可点击原型技法参考（Prototype Techniques）

> 来源吸收：Trae `interactive-demo-factory` 与 `flow2demo` 两个 skill 的成熟技法，以及用户自制 skill `pm-phase-4.5-prototype` 的「先出页面清单经人工确认、再生成单文件 HTML」分步流程，经适配后作为 page-design 的可选落地能力。
> 定位：page-design 产出页面骨架文本（§4 表格）是权威；本文档提供"把文字需求（页面骨架 + 交互/业务规则）转成可点击 HTML 原型"的技法，原型是 UX 的落地产物（UX 与原型一体），不是替代文本规则。
> 触发：当 P0 功能流程 ≥ 3 页、或涉及状态分支、或需要利益相关方评审视觉/交互时，生成可点击原型。**分两步走：先出页面清单经人工确认，确认后才生成单文件 HTML**；主流程与全部分支场景（含异常/边界态）都要覆盖。

## 1. 输入映射（pm-scaffold 语境）

> pm-phase-4.5-prototype 读取独立阶段文档（docs/2-用户故事、docs/3-用户旅程、docs/4-功能设计）；在 pm-scaffold 中，这些输入一律取自**已确认的上游产物**（product-ux.md 及下游功能设计），不读外部文档路径。

| 外部 skill 输入 | pm-scaffold 对应产物 | 说明 |
|---|---|---|
| 用户故事（US） | ST-XXX 故事（confirmed） | 页面动机与目标用户 |
| 用户旅程 | ux-flow §3.1/§3.2 已确认流程 | 页面顺序、入口边与分支 |
| 交互规则（IR） | interaction-rules 的 IX-XXX | 交互分支、弹窗、状态切换 |
| 业务规则（BR） | function-description 的 BR-XXX / VL-XXX / AC-XXX | 状态判定与分支去向 |
| 页面清单 | page-design §4.1 表格 | 原型页面节点，不发明页面 |

> **上游未 confirmed，不启动原型**：页面清单以 §4.1 为准；若 §4.1 缺失或未确认，先回流 page-design 补齐，不在原型阶段补页。

## 2. 输入与保真度

| 输入 | 必需 | 可接受格式 | 对产出的影响 |
|---|---|---|---|
| 页面清单 | ✅ | TXT / JSON / MD 列表 / Mermaid 节点 | 定义页面节点（page-design §4.1 表格可直接提供） |
| 流程描述 | ✅ | Mermaid / ASCII / 纯文本 / PDF / 图片 | 添加决策分支、状态机、跳转边 |
| UI 参考图 | ⭕ | PNG / JPG / Sketch / Figma 截图 | 升级为高保真布局 |
| 设计 token | ⭕ | CSS / JSON / Tailwind config | 覆盖默认品牌 |

保真度阶梯：仅页面清单 → 逻辑原型（低保真）；+流程 → 中保真（逻辑 100% 准确、占位 UI）；+UI 图 → 高保真。输入一律取自 §1 映射的上游产物，缺失项标 `待确认`，不自行补全。

## 3. 分两步工作流程（页面清单先行 → 单文件 HTML）

> 吸收自 pm-phase-4.5-prototype：先让页面清单经人工确认，再生成 HTML。页面清单是人工对"原型覆盖什么"的批准，没有这个确认就不写 HTML。

**Step 1 提取与页面清单确认**
1. **读需求**：从上游 product-ux.md 提取页面（§4.1）、功能清单（FEA-XXX）、交互规则（IX-XXX）、业务规则（BR/VL/AC-XXX）。
2. **出页面清单**：按表格列出待生成页面，每行可追溯到 FEA-XXX 与 §4.1 行；编号 P01 起。

| 页面编号 | 页面名称 | 涉及 FEA | 涉及 IX | 涉及 BR | 状态 |
|---|---|---|---|---|---|
| P01 | [名称] | FEA-001 | IX-001 | BR-001 | 待确认 |

3. **人工确认**：等待用户确认页面清单完整（页面无缺失、分支场景可覆盖）后才进入 Step 2；页面/交互/规则缺失时标 `待确认` 交人工补齐，不静默补页面、不替业务方选默认状态。

**Step 2 生成单文件 HTML**
4. **生成 HTML**：按确认后的页面清单，生成单一 HTML 文件（内联 CSS + JS，自包含、无外部依赖，浏览器直接打开即可演示）。
5. **落盘**：输出到 `requirements/REQ-XXX/99-review/support/prototype/`，命名 `[REQ-XXX-功能名]-交互原型-v1.0.html`。

## 4. 核心硬规则

1. **忠于输入，不发明状态**：分支目的地必须在页面清单中存在；若流程分支指向不存在的页面 → 停下来向人工报告，不静默补页面。
2. **多入口必须有演示入口页**：当存在多个起始页面（A/B/C/D）时，默认先做"演示入口"页，把各起始路径做成卡片，点击卡片进入对应起始页；不依赖侧边栏作为主路径。
3. **状态依赖目标页必须弹状态确认**：当点击结果取决于业务状态（已登录/未登录、已关注/未关注、已支付/未支付、权限角色、功能开关）时，用户点击真实入口后先弹"状态确认"弹窗，由演示者选择状态，再跳到对应目标页。弹窗选项用业务语言命名（如"已关注：基础页面 / 未关注：基础页面 + 关注模块"）。
4. **演示优先，不出现开发面板**：主 demo UI 中不出现右侧 AI 规则面板、调试面板、资源说明卡、路由表；AI 可读的规则、跳转矩阵、决策逻辑放在配套文档（如交互材料），不放进演示页。
5. **真实元素可点击**：页面内的真实元素（弹窗 CTA、账户行、通知卡）可点击驱动跳转，不只有侧边栏可点。
6. **缺失信息标 `待确认` 交人工**：页面、交互或规则信息缺失/未确认时，在页面清单与原型中显式标注 `待确认`，随交付物交人工决策，不静默补状态（与 `src/framework/contracts.md` 知识状态约定一致）。

## 5. 交付物结构

```text
requirements/REQ-XXX/99-review/support/prototype/
├── [REQ-XXX-功能名]-交互原型-v1.0.html  # 可点击 demo（自包含单文件，内联 CSS + JS，可直接打开）
├── assets/                              # 裁剪后的语义图片（复杂照片/品牌视觉用，普通 UI 用 HTML/CSS 重建）
├── demo-flow.md                         # 交互材料：页面清单、入口跳转逻辑、页面跳转矩阵、状态判定
└── render-check.png                     # 浏览器渲染验证截图（验证无空白页、文本不重叠、状态弹窗正常）
```

交互材料（demo-flow.md）建议结构：
- §1 页面与素材清单（与经人工确认的页面清单一致，含 涉及 FEA/IX/BR 列，不发明页面）
- §2 总体入口跳转逻辑
- §3 核心页面状态判断（各状态 → 目标页）
- §4 分入口流程说明
- §5 页面跳转矩阵（从 → 到 → 触发动作 → 条件）
- §6 后续 HTML/原型建议

## 6. 生成与验证流程

1. **解析输入**：从 product-ux.md §4.1 提取页面清单；从 §2 功能清单取 FEA-XXX；从 interaction-rules 的 IX 与 function-description 的 BR/VL/AC 提取分支与状态判定；冲突（分支指向不存在页面）先报告。
2. **出页面清单并确认**：按 §3 Step 1 生成清单表格（涉及 FEA/IX/BR），与 §4.1 一一对应；等待人工确认后才进入生成。
3. **构建页面图**：`pages[]` 数组，每页含 id、name、category（entry/auth/detail/success/error）、所属 FEA。
4. **重建 HTML**：不要用整页截图当背景图；导航、文本、卡片、按钮、列表用 HTML/CSS 重建；复杂图片（照片/地图/插画/品牌视觉）才用裁剪图。
5. **连接点击**：真实元素可点击；状态依赖目标页先弹确认弹窗；多入口有演示入口页。
6. **验证迭代**：浏览器渲染检查——无空白页、资源加载、文本不重叠、长页面可见、状态路由先问状态、多起始路径可发现、无调试面板；**主流程可完整走通**；**覆盖主流程 + 全部分支场景（正常/异常/边界/超时/取消/恢复）**，状态切换与 BR/VL/AC 一致；HTML 直接打开可用。
7. **回报假设**：报告任何未达目标的区域与假设，以及标 `待确认` 的项，交人工确认。

## 7. 边界（Do Not）

- 不生成生产代码（无真实数据库/后端调用）。
- 不替代 page-design §4 文本规则——原型是沟通工具，文本规则是权威。
- 不替业务方选择默认状态——状态依赖必须弹窗询问或显式标注 `待确认`。
- 视觉细节（动效、配色微调）不是本步骤目标，除非人工明确要求高保真。
- 原型只在 `99-review/support/prototype/` 落盘，不散落到工作区或项目其它路径。

## 8. 质量自检清单

- [ ] 页面清单先经人工确认，确认后才生成 HTML
- [ ] 页面清单与 page-design §4.1 一一对应，无发明页面
- [ ] 每个分支目的地存在于页面清单
- [ ] 主流程可完整走通
- [ ] 全部分支场景（正常/异常/边界/超时/取消/恢复）可演示
- [ ] 状态切换与 BR/VL/AC 一致
- [ ] 多入口项目有演示入口页
- [ ] 状态依赖目标页弹状态确认弹窗（业务语言命名）
- [ ] 主 demo 无调试/规则/路由面板
- [ ] 页面内真实元素可点击（不只侧边栏）
- [ ] 缺失信息已标 `待确认` 并交人工
- [ ] 浏览器验证截图无空白页、无文本重叠
- [ ] HTML 可直接打开（自包含，无外部依赖）
- [ ] 原型路径与 PRD 中引用的版本一致

## 9. 参考图还原技法（image2html）

当输入含 UI 参考图/竞品截图（走高保真路径，§2 的 UI 参考图输入）时，按「真实页面结构」还原，而非整图贴背景：

1. **核心规则**：不得把整张参考图作为页面背景或一张大 `<img>` 贴上去；页面主体（导航、卡片、按钮、筛选、表格、侧边栏）一律用 HTML/CSS 重建。位图资产只用于天然需要的地方：照片、地图/卫星图、复杂插画、截图内嵌图、品牌视觉；图标用 SVG/CSS，不从截图裁。
2. **还原工作流**：分析参考图（画布尺寸、字体字号、色板、边框阴影、间距规律）→ 拆分为区域（header/侧边栏/内容/卡片/表单/表格/图表）→ 决定哪些区域用 HTML/CSS、哪些用裁剪图（裁剪图按语义命名入 `assets/`）→ 逐区域重建 → 同视口尺寸浏览器截图与参考图对比 → 迭代（间距/字号/卡片尺寸/边框/颜色/表格密度）→ 直至视觉接近且结构真实。
3. **结构真实与可交互**：交互感元素（按钮、标签页、表行、入口卡片）保持为真实可交互元素（结构可点，即使无需真实行为）；字段多的表格允许横向滚动容器 + 关键操作列 sticky，动作链接直接可见，不藏省略号。
4. **标注义务**：参考图是证据（FACT），但「照着还原的布局是否满足业务需求」未确认——还原区域与解读必须标 **AI_INFERENCE**，随原型交人工确认；竞品截图登记 SRC-* 进入来源追溯；还原中缺失/存疑处标 `待确认`。
5. **保真度边界**：参考图还原只提升视觉保真，不改变逻辑保真——页面清单以经人工确认的清单（§3 Step 1）为准，分支与状态判定以 BR/VL/AC 为准，不因参考图发明页面或状态。

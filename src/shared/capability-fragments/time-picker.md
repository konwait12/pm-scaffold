# 能力片段：时间/年月选择组件（time-picker）

> 跨案例通用能力标准片段（组件型 FUN）。**复制到 functional-flow / business-rules / acceptance-criteria 时改 FUN 编号**（示例为 FUN-XXX，目标产物需重新编号，避免与既有 ID 冲突）。
>
> 覆盖：半屏弹窗 + 滑动选择年月、默认当前年月、跨页面范围声明、（业务扩展）时间合法性规则。
>
> **来源案例字段说明**：表中「源 REQ-XXX」为历史溯源，仅用于追溯片段口径来源；新场景复用时可直接沿用标准行，无需替换来源案例编号。

## 标准 BR 表行（4 条）

| ID | 规则描述 | 类型 | 触发条件 | 约束/逻辑 | 所属 FUN | 来源 |
|---|---|---|---|---|---|---|
| BR-XXX-1 | 组件交互形态：半屏弹窗 + 滑动选择 | 交互 | 点击时间/年月按钮 | 从页面底部弹出半屏弹窗；滑动选择年/月；点击确认后弹窗关闭、所选时间生效 | FUN-XXX | capability-fragments/time-picker（源 REQ-008 功能流程「半屏弹窗→滑动选择年月→确认」） |
| BR-XXX-2 | 默认值：当前年月 | 规则 | 进入选择/看板初始态 | 默认选中当前年份与月份作为初始统计/展示时间；进入页面即按默认值出数 | FUN-XXX | capability-fragments/time-picker（源 REQ-008 BR-025） |
| BR-XXX-3 | 跨页面范围声明 | 规则 | 组件被多板块复用 | 明确声明组件覆盖范围（如「涉及 A/B/C/D/E 五个板块」），各板块共用同一时间口径，保证选择一致生效 | FUN-XXX | capability-fragments/time-picker（源 REQ-008 BR-024「涉及 OMNI TTL/BTQ/DS/Online/品类表现五个板块」） |
| BR-XXX-4 | 业务侧时间合法性规则（扩展行，按需启用） | 校验 | 定时/计划类场景提交时间 | 如「开始 ≥ 当前+10 分钟；结束 ≥ 开始+10 分钟」，不满足→报错不提交——此为业务扩展，非组件默认行为 | FUN-XXX | capability-fragments/time-picker（源 REQ-006 BR-015 / EX-006） |

## 标准 AC 行（3 条）

| ID | 验收标准（Given/When/Then） | 量化阈值 | 来源目标 G | 所属 FUN | 优先级 |
|---|---|---|---|---|---|
| AC-XXX-1 | Given 用户点击时间选择按钮, when 滑动选择年月并确认, then 弹窗关闭且对应页面展示所选时间数据、时间框显示所选年月 | 100% 执行 | G-XXX | FUN-XXX | P0 |
| AC-XXX-2 | Given 用户首次进入页面, when 时间组件展示, then 默认选中当前年份与月份 | 100% 默认 | G-XXX | FUN-XXX | P0 |
| AC-XXX-3 | Given 用户在时间弹窗中, when 点击取消或蒙层, then 弹窗关闭且所选时间不生效 | 100% 交互 | G-XXX | FUN-XXX | P0 |

## 标准 EX 行（2 条）

| ID | 场景 | 触发条件 | 系统行为 | 恢复方式 | 用户提示 | 所属 FUN |
|---|---|---|---|---|---|---|
| EX-XXX-1 | 时间选择取消 | 蒙层点击 / 取消操作 | 关闭弹窗，所选时间不生效 | 重新选择 | 弹窗关闭（无额外提示） | FUN-XXX |
| EX-XXX-2 | 时间不合法（扩展，按需启用） | 开始时间过近 / 结束早于开始+最小间隔 | 报错，不提交 | 调整时间 | 「时间不合法」类报错 | FUN-XXX |

## 说明

- **适用场景**：需要「年月/时间」粒度的选择器组件，尤其是**数据看板按时间筛选**（跨板块统一口径）或**定时/计划类提交**（带合法性校验）场景。组件默认只负责「半屏弹窗 + 滑动选择 + 默认当前年月 + 范围声明」四件事，业务合法性规则作为扩展行按需启用。
- **来源案例**：
  - REQ-008-bae（FUN-009 时间选择组件）：FEA-009（跨 5 板块年月选择，默认当前年月）/ BR-024（时间选择范围）/ BR-025（时间默认值）/ STATE-006（滑动选择+确认→时间生效）/ EX-005（取消）/ AC-013 / 字段定义（默认当前年月、滑动选择确认后生效）。
  - REQ-006-service-account（FUN-008 定时发送时间）：BR-015（定时发送 10 分钟规则）/ EX-006（定时时间不合法）/ AC-012 / 字段定义（开始 ≥ 当前+10 分钟；结束 ≥ 开始+10 分钟）——作为「业务侧合法性规则」扩展行的来源。
- **使用方式**：复制到 functional-flow / business-rules / acceptance-criteria 对应章节表格时——① 将 `FUN-XXX` 改为目标产物中的实际 FUN 编号（如 FUN-009）；② BR/AC/EX ID 改为目标产物内单调递增编号；③ 「来源」列保留本片段路径，并可追加原始 SRC 追溯；④ 跨页面范围声明必须按目标案例实际列出覆盖板块，不得照抄；⑤ 仅当目标案例为定时/计划类提交时启用 BR-XXX-4 / EX-XXX-2 扩展行，否则删除并注明。
- **知识状态**：BR-XXX-1~3 为 FACT（组件交互形态与默认值来自已跑测案例）；BR-XXX-4 为案例特定业务规则，启用时须由业务事实负责人确认口径。
- **关联**：组件型 FUN 规则密度天然不足（BR < 3 触发 rule_density HIGH 告警），本片段自带 4 条 BR 满足密度下限，对应 OBS-008 优化方向。

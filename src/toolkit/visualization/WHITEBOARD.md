# Whiteboard Tool

Use the local whiteboard to visualize user journeys, UX flows, state models or traceability when a reviewer requests an editable canvas. It is an optional rendering tool, not a Skill and not a workflow stop.

Keep node labels tied to artifact IDs, preserve the source Markdown/table as authority, and export or sync only after verifying that the board renders correctly.
